Scratch list. Anything here that turns out to be more than an afternoon gets
worked through in `ideas/` and taken off this list — this file is the inbox, not
the plan. `ideas/README.md` is the plan.

Remove fully completed entries when we have done them or moved them to other files to not waste time reading solved things. Strike-through is only useful if something is half-done.

## Open

- `compact` and `set_name` are engine words no shipped game writes. Both were
  built for a game that then did not use them: `compact` for Arnak's market row
  closing its gap, `set_name` for Spellstorm's chosen wizard naming the seat
  card. Both still have integration tests, so they work — they are just not
  wired into the game that asked for them. Use them there or drop them; an
  action nothing writes is a word the format charges for and nobody spends.
