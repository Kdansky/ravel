Scratch list. Anything here that turns out to be more than an afternoon gets
worked through in `ideas/` and taken off this list — this file is the inbox, not
the plan. `ideas/README.md` is the plan.

Remove fully completed entries when we have done them or moved them to other files to not waste time reading solved things. Strike-through is only useful if something is half-done.

## Open

A grid zone still draws its label under the first card played into it — a hand no longer does. LoR's "Bench" is the case. Same fix, different module: a grid's cells come from `zones.cell_rect`, not from `card_places`.

A badge always draws an icon, so Splendor's six token piles all wear the fallback diamond beside their count. The plate colour says which gem it is, so this is clutter rather than a lie — but a badge that is only a number has no spelling.
